<?xml version="1.0" encoding="utf-8"?>
<com.anakarwin.apples.SquareCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="@dimen/item_margin_small"
    android:background="?attr/selectableItemBackgroundBorderless"
    android:clipToPadding="true"
    android:padding="@dimen/item_margin_small"
    app:cardBackgroundColor="@color/accent"
    app:cardCornerRadius="@dimen/item_card_radius">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:orientation="vertical"
        android:padding="@dimen/item_margin_small">

        <ImageView
            android:id="@+id/icon"
            android:layout_width="@dimen/item_image_size_small"
            android:layout_height="@dimen/item_image_size_small"
            android:layout_gravity="center"
            android:layout_marginBottom="8dp"
            android:contentDescription="@string/dashboard_icon"
            tools:src="@drawable/ic_schedule"/>

        <TextView
            android:id="@+id/title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center"
            android:textColor="@color/primary_text"
            android:textSize="@dimen/item_title_text_size"
            android:textStyle="bold"
            tools:text="@string/dashboard_presents"/>

    </LinearLayout>
</com.anakarwin.apples.SquareCardView>